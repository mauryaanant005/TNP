"""Empty — `CategoryRule` moved to `placements/` in T-19.

Kept for its applied migrations only; see the note in `staff/models.py` for
why deleting the package outright would break `migrate` on a live database.
"""
