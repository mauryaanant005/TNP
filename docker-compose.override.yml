services:
  api:
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/app/data
  celery:
    volumes:
      - sqlite_data:/app/data
  frontend:
    ports:
      - "5173:80"

volumes:
  sqlite_data:
